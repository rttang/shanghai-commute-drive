---
name: 上海漫游
description: 全屏上海观光与克制的外围操作界面
colors:
  paper: "#f3f1e8"
  ink: "#263d35"
  green: "#476e59"
  river: "#416f70"
  muted: "#617268"
typography:
  display:
    fontFamily: '"Songti SC", "STSong", serif'
    fontSize: "72px"
    fontWeight: 500
    lineHeight: 1.18
  body:
    fontFamily: '"PingFang SC", sans-serif'
    fontSize: "13px"
    lineHeight: 1.9
---

# 上海漫游设计系统

方向由用户明确指定：一江两岸的真实风景、两条观光路线、高保真汽车。以实际三维场景作为主画面，首页把路线选择置于左下、座驾置于右下；驾驶时保留外围地图和底部控制。宋体标题与无衬线操作文字区分观赏与操控。

界面使用低饱和石色、深绿与细线。三维场景采用 PBR 沥青、HDR 环境光、玻璃与金属材质、日间太阳、阴影和雾；高清档追加环境遮蔽。主画面不能被控制栏遮挡，手机竖屏需独立检查汽车和道路可见性。

只提供两条路线，不保留挑战、计分、解锁或失败惩罚。开始观光、手动驾驶、暂停、换车和视角切换都有明确中文标签。弹窗采用原生 dialog，关闭后恢复焦点与先前游览状态；触屏按 pointerId 释放。

汽车模型按实车多视角资料判断。SU7、Model 3、海豚已接入2024款详细外观网格，其余七款程序化车辆仍属于未通过外观验收的原型。2026-09-09用户批准补充独立轮轴和自由手动驾驶，界面仅调整驾驶说明与离路提示；十款车轮动画完成不能代替十款真实外观验收。不要把旧模型换色、增加细分或改名描述为高保真完成。模型年份、来源和许可在车辆参考中明确展示。

当前桌面与手机字号尚需统一验收，不把较小的辅助署名字号作为后续设计默认值。此文件记录现有设计方向，不是已通过全部视觉验收的证明。

街景使用 Dream Loop 的当前截图生成目标、Blender建模与纹理、同机位实际截图复查方法。目标图用于材质和层次方向，上海建筑位置继续服从OSM数据；目标图生成的装饰细节不是实景测量证据。共享石材与铺装纹理、带透明叶片的分枝树冠、窗框线脚、江岸栏杆、PBR水面与日间光照构成当前场景。独立入口为 npm run streets。


## 近处街景的真实性

外滩十二栋沿街建筑各有独立照片、立面纹理和特征结构；东方明珠补充三组斜支撑、玻璃球带、入口与基座，正大广场采用弧形立面和独立玻璃幕墙。浦东另有海洋水族馆的三角形帆墙、恒生银行的石材与中央玻璃竖带、东亚银行的错层塔身。窗洞回退、实体柱廊、拱廊深度、线脚与穹顶必须在斜视角能辨认，不能只依赖正面贴图。近处重点建筑不再使用共享窗格楼体。远处普通建筑继续使用轮廓、有限材质和雾控制成本。

照片与生成结果分开保存。生成立面允许去除树木、车辆和透视，但不证明补全部位的测绘准确性；未见侧面、复杂雕塑和部分楼高仍有推测。采用实拍的拍摄日期和许可必须随资产保留。十二栋立面、两条游览路线、三个详细车辆属于不同验收范围，不用一项通过代替另一项。

固定机位验收先在1280×720、DPR1、均衡档采样600帧，再检查近处斜视角和浦东全景。Apple Silicon上的自动浏览器通过 arch -arm64 明确选择Chrome架构，同时使用ARM原生Node。仅更换父进程还不足以消除架构继承。性能目标是桌面60FPS，低帧样本和环境差异保留，不当作通过记录删除。

## 模型预算与驾驶反馈

普通模型按单个平均10MB以下控制，著名地标允许更大；不设整套街景10MB限制，不通过填充空数据扩大文件。实拍建筑保留原图尺寸、JPEG96和18/14/16位几何编码，汽车最大纹理4096、JPEG95。源图、生成图、源包和可编辑工程保留在既有原路径与移动硬盘，不再自动删除生成图像。

手动驾驶依据自身朝向、轴距、油门、滑行阻力、制动和转角行进；不能用每帧路线投影纠正位置。只在车道附近同向行驶时允许自动接管，离路时提示返回或重新开始。车轮按实际半径滚动，前轮内外转角不同；停车不自转，镜头与小地图跟随独立车辆位置。当前不包含碰撞、倒挡、悬架和轮胎滑移。

## 2026-09-09 已确认的环游体验更新

本节替代此前的“两路线、不设挑战评分、不含碰撞倒挡”限制。主体验是一条连续跨江环线，沿实景道路探索上海。首页突出环游与手动出发，自动观光作为并列入口。行驶时前景保持清晰，任务面板显示下一处风景、停车提示、相册数量和安全分；详细介绍与收藏画面在相册中展开，不把开发进度或资产预算放入游玩界面。

保留墨绿、暖纸色和石材色的现有视觉方向，标题用宋体，操作文字清楚克制。观景位用真实尺度的停车线与小型标牌，避免大面积悬浮标志挡住建筑。日景、暮色和夜景服务于观察上海；驾驶音量保持轻，暂停同步静音。高保真以参考照片和实际侧视细节验收，未通过的资产继续列为待完成。

桌面 W/S、A/D、Q、F、R、C、P、J；触屏保留油门、刹车、左右转向并提供换挡、拍照和救援按钮。相册、存储失败、加载失败和后台暂停都有可恢复操作。完整环线、碰撞、高程、观光任务、来源对照与当前设备性能分别检验，阶段通过后继续完成后续工作。
